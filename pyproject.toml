[build-system]
requires = ["setuptools>=68"]
build-backend = "setuptools.build_meta"

[project]
name = "retinal-retfound"
version = "0.1.0"
description = "RETFound fine-tuning code for retinal image experiments"
requires-python = ">=3.11"

[tool.setuptools.packages.find]
where = ["src"]
